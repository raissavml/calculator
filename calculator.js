const calculator = document.querySelector('calculator');
const funtionalities = caculator.querySelector('functionalities');
funtionalities.addEventListener('click', e => {
    if (e.target.matches('button')) {
        
    }
})